services:
  podcast-cutter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: podcast-cutter
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
